# Environment variables for the backend application
# Server
PORT=5000

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=tender_management_db
DB_DIALECT=mysql

# Optional: JWT or session secret (if using auth)
JWT_SECRET=your_jwt_secret

